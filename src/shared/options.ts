// Shared option types used across feature filters and pickers.
export type CustomerOption = {
  customerNo: string
  customerName: string
  customerGroup: string
  groupName: string
}

export type ItemOption = {
  itemNo: string
  itemName: string
}

export type LocationOption = {
  code: string
}

export type PalletAddressOption = {
  code: string
}
